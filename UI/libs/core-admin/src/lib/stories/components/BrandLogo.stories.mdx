<!-- BrandLogo.stories.mdx -->

import { Canvas, Meta, Story, Source } from '@storybook/addon-docs';
import dedent from 'ts-dedent';

import BrandLogo from '@core-admin/components/BrandLogo';

<Meta
  title="MDX/BrandLogo"
  component={BrandLogo}
/>

# BrandLogo

With `MDX`, we can define a story for `BrandLogo` right in the middle of our Markdown documentation.

<Canvas>
  <Story name="all BrandLogos">
    {() => {
      return {
        components: { BrandLogo },
        template: `
          <div>
        <v-form>
          <BrandLogo />
        </v-form>
  </div>`,
      };
    }}
  </Story>
</Canvas>

## Brand-able settings will be saved

<Source
  language="js"
  dark
  format={true}
  code={dedent`
function getFormValues(submitEvent) {
  // Push model JSON to API endpoint for Public App consumption"
  console.log(formJson(submitEvent.target));
}
`}
/>
;

## With Component

<Source id="mdx-brandlogo--all-brand-logos" />
